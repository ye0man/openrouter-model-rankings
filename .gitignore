# Local environment
.env
.env.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
env/
venv/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Local artifacts (generated files live in public/ and data/ but are committed
# for GitHub Pages deployments that don't run Actions, e.g. local preview).
# Keep data/*.json tracked so local builds work offline; remove if preferring
# GitHub Actions-only artifacts.
# data/*.json
